import 'package:flutter/material.dart';

class AuthStateTest extends StatelessWidget {
  const AuthStateTest({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Đăng nhập thành công'),
      ),
    );
  }
}
